__pycache__/
*.py[cod]
*$py.class
.venv/
venv/

# 便携：嵌入式 Python 运行时（勿提交）
python/
scripts/portable/get-pip.py
.env
*.egg-info/
.pytest_cache/
.streamlit/secrets.toml

# IDE / local
.cursor/
.vscode/

# 运行时用户账号（含密码哈希）
数据/users.json

# PyInstaller
build/
dist/

# 一键便携包构建输出（内含 embeddable + .venv，体积大）
dist_portable/

# 本地生成的服务器上传包（可重新生成）
deploy_upload/

# Historical milestone zips (keep on disk; not duplicated in Git)
archives/

# 发布 zip 由 scripts/make_release_zip.py 本地生成，避免误提交或锁定冲突
releases/*.zip
releases/*.zip.tmp
